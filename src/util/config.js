export const simpleRandom = 5;
export const timeLocation = 8;
export const cluster = 9;
export const systematicRandom = 10;
export const api = ''
export const startId = 1;

export const config = {
    simpleRandom,
    timeLocation,
    cluster,
    systematicRandom,
    api,
    startId
};

export default config